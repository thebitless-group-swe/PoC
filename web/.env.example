# URL base assoluta del backend FastAPI (senza slash finale).
# La fetch del riassunto e cross-origin diretta su :8000, quindi il backend
# deve avere questa origine in CORS_ORIGINS. Copiare in web/.env per il dev locale.
VITE_API_BASE_URL=http://localhost:8000
